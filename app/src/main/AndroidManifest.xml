<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tshop.t_shop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Orders.OrdersActivity"></activity>
        <activity android:name=".AboutAppActivity" />
        <activity
            android:name=".Products.PreviewDialogActivity"
            android:theme="@style/Theme.UserDialog" />
        <activity android:name=".Basket.BasketActivity" />
        <activity android:name=".Products.ProductsActivity" />
        <activity android:name=".Auth.RegistrationActivity" />
        <activity android:name=".Auth.ConfirmationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartActivity" />
        <activity android:name=".Help.HelpActivity" />
        <activity android:name=".Help.HelpDescriptionActivity" />
        <activity android:name=".Auth.PhoneAuthActivity" />
    </application>

</manifest>